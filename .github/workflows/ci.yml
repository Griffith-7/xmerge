name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  PYTHON_VERSION: "3.12"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install deps
        run: |
          pip install --upgrade pip
          pip install ruff
      - name: Lint
        run: ruff check src/ tests/

  test-cpu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install deps
        run: |
          pip install --upgrade pip
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install transformers datasets numpy pytest
          pip install -e .
      - name: Run CPU tests
        run: python -m pytest tests/ -v -m "not slow" --timeout=120 2>&1 | head -100

  test-gpu:
    runs-on: ubuntu-latest
    if: false  # disabled until a self-hosted GPU runner is available
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install deps
        run: |
          pip install --upgrade pip
          pip install torch transformers datasets numpy pytest pytest-timeout
          pip install -e .
      - name: Run all tests
        run: python -m pytest tests/ -v --timeout=300
